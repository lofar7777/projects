package com.pyqhub.entity;

public enum Role {
    STUDENT,       // Can only view questions
    CONTRIBUTOR,   // Can add and edit their own questions
    ADMIN          // Full control over all resources
}
